trigger:
- none

variables:
#- group: 'faxe_dev - Devcloud - secret' # variable group
- name: trivyVersion
  value: '0.9.1'

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Setup
  jobs:
  - job:
    steps:
    - checkout: self
#    - task: Docker@2
#      inputs:
#        containerRegistry: 'nexus-docker-connection'
#        command: 'login'
#      displayName: registry log in 
#    - task: Docker@2
#      inputs:
#        repository: 'faxe'
#        command: 'build'
#        Dockerfile: 'Dockerfile_debian'
#        tags: |
#          latest
#          $(Build.BuildNumber)
#          -dev-$(Build.BuildNumber)
#    - task: Docker@2
#      inputs:
#        containerRegistry: 'nexus-docker-connection'
#        repository: 'faxe'
#        command: 'buildAndPush'
#        Dockerfile: 'Dockerfile_debian'
#        tags: |
#           $(Build.BuildNumber)
#           dev-$(Build.BuildNumber)
#      displayName: build container image
    - task: Docker@2
      inputs:
        containerRegistry: 'nexus-docker-connection'
        command: 'login'
      displayName: docker login
    - script: |
        echo ---------------------
        docker images
        wget https://github.com/aquasecurity/trivy/releases/download/v$(trivyVersion)/trivy_$(trivyVersion)_Linux-64bit.deb
        sudo dpkg -i trivy_$(trivyVersion)_Linux-64bit.deb
        docker images
        #trivy --debug -severity HIGH,CRITICAL faxe:20210409.3
        trivy --debug -severity HIGH,CRITICAL faxe:$(Build.BuildNumber)
        echo --------------
        echo $(pwd)
        trivy filesystem  -severity HIGH,CRITICAL /home/vsts/work/1/s
        echo --------------
        #trivy repository -severity HIGH,CRITICAL docker.datahub.tgw-group.cloud:443/faxe:20210409.3
        #echo ---------------------
      displayName: install trivy on build server
    - script: |
        docker run --rm -v /var/run/docker.sock:/var/run/docker.sock  aquasec/trivy:0.16.0 --severity HIGH,CRITICAL docker.datahub.tgw-group.cloud:443/faxe:20210409.3
      displayName: use trivy via docker
    #- task: Docker@2
    #  inputs:
    #    containerRegistry: 'nexus-docker-connection'
    #    repository: 'faxe'
    #    command: 'push'
    #    tags: '$(Build.BuildNumber)'
#  - job:
#    steps:
#    - checkout: self
#    - script: | 
#        echo '$(Build.Repository.Name).$(Build.BuildNumber)'
#        echo $(FAXE-CRATE-HTTP-PASS-KeyVault-Test)
#    - task: Docker@2
#      inputs:
#        containerRegistry: 'nexus-docker-connection'
#        repository: 'faxe'
#        command: 'buildAndPush'
#        Dockerfile: 'Dockerfile_debian'
#        tags: |
#           $(Build.BuildNumber)
#    - script: |
#        sudo apt-get install rpm
#        wget https://github.com/aquasecurity/trivy/releases/download/v$(trivyVersion)/trivy_$(trivyVersion)_Linux-64bit.deb
#        sudo dpkg -i trivy_$(trivyVersion)_Linux-64bit.deb
#        trivy -v
#  displayName: 'Download and install Trivy'
